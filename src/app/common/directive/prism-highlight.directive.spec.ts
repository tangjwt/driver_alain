import { PrismHighlightDirective } from './prism-highlight.directive';

describe('PrismHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PrismHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
